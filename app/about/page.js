import React from 'react';

const About = () => {
    throw new Error('Error from about page');
    return (
        <div>
            this is about page
        </div>
    );
};

export default About;